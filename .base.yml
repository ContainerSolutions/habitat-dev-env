version: '2'
services:
  base:
    build:
      context: .
      args:
        uid: 1000
    image: habitat
    volumes:
      - .:/mnt
    working_dir: /mnt/core-plans/cassandra
    stdin_open: true
    tty: true
    command: /bin/sh
    cap_add:
      - SYS_ADMIN

